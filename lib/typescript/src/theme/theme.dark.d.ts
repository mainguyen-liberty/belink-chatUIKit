import type { ReleaseArea } from '../types';
import type { Palette, Theme } from './types';
/**
 * Create a dark Theme.
 * @param palette - The palette of the Theme. {@link Palette}
 * @returns The dark Theme.
 */
export declare function createDarkTheme(palette: Palette, releaseArea?: ReleaseArea): Theme;
/**
 * Create a dark Theme.
 * @param palette - The palette of the Theme. {@link Palette}
 * @returns The dark Theme.
 */
export declare function useDarkTheme(palette: Palette, releaseArea?: ReleaseArea): Theme;
//# sourceMappingURL=theme.dark.d.ts.map