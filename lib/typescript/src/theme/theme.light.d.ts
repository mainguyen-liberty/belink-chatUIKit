import type { ReleaseArea } from '../types';
import type { Palette, Theme } from './types';
/**
 * Create a light Theme.
 * @param palette - The palette of the Theme. {@link Palette}
 * @returns The light Theme.
 */
export declare function createLightTheme(palette: Palette, releaseArea?: ReleaseArea): Theme;
/**
 * Create a light Theme.
 * @param palette - The palette of the Theme. {@link Palette}
 * @returns The light Theme.
 */
export declare function useLightTheme(palette: Palette, releaseArea?: ReleaseArea): Theme;
//# sourceMappingURL=theme.light.d.ts.map